# v102 屏幕节电测试版实施计划

## 隔离与版本管理

- 所有修改、构建、测试记录和交付产物仅保存在 `TW66GW02/12`。
- `TW66GW02/11` 为只读基线，通过前后 SHA-256 清单确认未被修改。
- GitHub 分支为 `test/v102-display-power`，先提交目录 11 的程序基线，再单独提交 v102 节电改动。

## 固件修改

- C8721 恒流档由 10 mA 调整为 8.5 mA。
- 点亮段 PWM 由 `0xFF` 调整为 `0xD0`。
- 理论 LED 导通电流比例为 `8.5 / 10 * 208 / 255 = 69.3%`，目标节电约 30%。
- 显示内容、模式、图标、10 ms 显示任务、分段发送、按键和关机逻辑保持不变。
- OTA 构建脚本增加 `-OtaOnly`，生成 v102 时不得覆盖 v100 工厂固件。

## 交付与验收

- App 使用 `mathis_ota_v102.ota` 及对应 manifest 中的大小、CRC 和目标版本 102。
- 控制器升级前烧录原有 `mathis_factory_v100.bin` 到 `0x08000000`。
- 自动验证 v100 工厂固件哈希不变，并验证 OTA 文件、manifest 大小、CRC 和版本一致。
- 实机需在相同显示内容和供电条件下比较 60 秒平均功耗，并检查无闪烁、重影或缺段。
- 若实测屏幕支路降幅不足 25%，仅将 PWM 下调到 `0xC0` 后重新生成最终 v102。

> 当前 OTA 签名区为开发占位，v102 仅用于测试，不作为量产安全固件。
