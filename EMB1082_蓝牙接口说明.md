# EMB1082 蓝牙接口说明

## 1. 职责划分

根据 Mathis 规范和联调信息，EMB1082 在本机中承担 BLE 无线链路与 UART 透明传输功能；STM32 是 Mathis 应用协议的处理方。

STM32 负责：

- 读取四路温度、电量、单位、固件版本和错误状态。
- 组装完整 Mathis 帧，包括帧头、类型、序号、长度、Payload 和 CRC-16。
- 在蓝牙连接后每秒生成一次 `TELEMETRY`。
- 校验并执行 App 下发的六个命令：切换单位、关机、三段系数更新、恢复出厂。

EMB1082 负责：

- 在 BLE 与 STM32 UART 之间透传完整 Mathis 字节流。
- 把 App 发出的 Mathis 框架完整透传给 STM32。
- 将 STM32 发出的 20 字节遥测帧作为一次 BLE 数据发送；该帧可放入默认 MTU 23 的 20 字节有效载荷。

EMB1061 不负责生成 Mathis 帧、计算 CRC、采集温度或执行业务命令。

## 2. Telemetry START/STOP 控制

`FF 01` 和 `FF 00` 不是 UART 裸字节事件，必须位于一个 CRC 正确的 Mathis `CMD` 框架 Payload 中：

| CMD Payload | 含义 | STM32 动作 |
|---|---|---|
| `FF 01` | `TELEMETRY START` | 立即启用遥测计时，此后每 1 秒发送一个 `TELEMETRY` |
| `FF 00` | `TELEMETRY STOP` | 停止遥测，清空待发送队列和未完成系数更新 |

以 Sequence=`00` 为例，完整 START/STOP 输入分别为：

```text
START: 5A 5A 01 00 02 00 FF 01 06 B5
STOP : 5A 5A 01 00 02 00 FF 00 27 A5
```

对于 App Mathis 命令，STM32 必须先验证起始标记、Type、Length 和 CRC，只有 `Type=01`、`Length=2` 且 Payload 严格为上述值时才改变遥测状态。Mathis 协议解析器不会把 Payload 中其他位置出现的 `FF` 当作控制命令。模块自身产生的独立订阅事件由下面的传输适配层规则处理，不属于 App Mathis 命令。

实机联调发现 EMB1082 存在两种模块固件行为：一种把 App 的完整 Mathis 数据透明传给 STM32；另一种会在 TX Notify 订阅状态变化时，额外从 UART 输出独立的两字节模块事件 `FF 01`/`FF 00`。后者不是 App Mathis 消息。为兼容两种版本，STM32 仅在 Mathis 流式解析器空闲、一次 UART IDLE 数据块长度严格等于 2 时识别该模块事件；若 `FF 01`/`FF 00` 位于正在接收的完整 Mathis 框架中，仍按 Length 和 CRC 正常解析，不会被误判为模块事件。

## 3. Mathis 遥测帧

完整帧固定为 20 字节：

```text
5A 5A | 81 | Seq | 0C 00 | 12-byte Payload | CRC16-LE
```

Payload 顺序：

```text
Mode(1) + Cavity(2) + Left/O(2) + Right/D(2) + Probe(2)
+ Battery%(1) + Firmware(1) + Error(1)
```

CRC 使用 CRC-16/CCITT-FALSE，覆盖 `Type + Seq + Length + Payload`，多字节字段均为小端序。标准校验向量为：

```text
CRC16("123456789") = 0x29B1
```

## 4. 当前工程实现

- `BSP/wireless.c`：Mathis 流式解析、框架内 START/STOP、业务命令、1 Hz 遥测和 UART DMA 发送队列。
- `BSP/wireless.h`：Mathis 协议、`TELEMETRY START/STOP` Payload 常量。
- `COP/main_control.c`：蓝牙状态机、遥测快照、单位与系数持久化。
- `Core/Src/main.c`：每 20 ms 调度无线任务。

模块的 GATT UUID、广播包、安全配对和单连接限制不由 STM32 工程配置；当前工程继续按“EMB1082 已预配置为透明传输模块”的边界处理，相关配置需用模块配置资料和实机验证。

## 5. 串口与联调观察点

- 蓝牙模块使用 `COM1` / `USART1`，参数为 115200、8N1，引脚为 PA9(TX)、PA10(RX)。
- `COM2` / `USART2` 是温度板串口，参数为 9600、8N1。`COM2.rxBuf` 中的 `FC ...` 数据不是蓝牙数据，不能据此判断 START 是否到达 STM32。
- UART DMA 工作区是 `COM1.dmaBuf`；IDLE 中断后，完整数据会复制到稳定快照 `COM1.rxBuf`，再由 20 ms 无线任务读取，避免 DMA 重启覆盖尚未解析的帧。

Keil Watch 可加入 `g_mathis_ble_debug`。收到一次合法 START 后，预期如下：

| 字段 | 预期变化 |
|---|---|
| `uart_chunks` / `uart_bytes` | 至少增加 1 / 10 |
| `frames_ok` / `cmd_frames` | 各增加 1 |
| `last_type` / `last_payload_length` | `01` / `2` |
| `last_command` / `start_frames` | `FF` / 增加 1 |
| `telemetry_enabled` | 变为 1，蓝牙图标随后常亮 |
| `telemetry_queued` / `telemetry_tx_started` | START 后约 1 秒开始增加，此后约每秒增加 1 |

若模块输出独立的两字节订阅事件，则 `frames_ok` 和 `start_frames` 不增加，而 `module_start_events` 增加、`telemetry_enabled` 变为 1；收到独立 `FF 00` 时对应增加 `module_stop_events`。

若 `uart_chunks` 始终为 0，说明 App 数据没有到达 STM32 的 USART1，应检查写入的 GATT RX 特征、模块透明传输配置、115200 波特率和 PA9/PA10 接线。若 `crc_errors` 增加，则检查实际收到的 10 字节和 CRC。`COM1.overrunCount` 非零表示上一数据块尚未取走时又收到新块。

### EMB1082 引脚与 GATT 核对

- App 必须向 Mathis 服务下的 `FFE1`（RX，App → Device）写二进制数据，并订阅 `FFE2`（TX，Device → App）的 Notify。
- EMB1082 Pin 16 `P3_0/UART_TX` 应连接 STM32 PA10/USART1_RX。
- STM32 PA9/USART1_TX 应连接 EMB1082 Pin 15 `P3_1/UART_RX`。
- EMB1082 Pin 11 `P0_3/LOG_UART_TX` 只输出模块调试日志，不得作为透明 UART_TX 接入 STM32。

若 `COM1.rxBuf` 出现 `Static Random Addr` 等 ASCII 文本，而发送 Mathis 帧后 `uart_bytes` 不增加、`frames_ok` 仍为 0，说明当前观察到的是模块调试日志，BLE RX 数据没有通过用户透明串口到达 STM32。应优先核对上述引脚和模块的 `AT+SERVUUID`、`AT+RXUUID`、`AT+TXUUID`、`AT+BAUD` 配置，而不是修改 Mathis CRC 或帧解析器。

## 6. 烧录文件

Keil 原始输出目录为 `MDK-ARM/tw66gw02/`。工程已增加 After Build 同步，因此每次成功构建后，下列两个 HEX 内容相同：

```text
MDK-ARM/tw66gw02/tw66gw02.hex
MDK-ARM/tw66gw02.hex
```

这样可避免误烧根目录中遗留的旧版 HEX。
