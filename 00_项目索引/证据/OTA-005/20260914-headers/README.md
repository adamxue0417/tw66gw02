# OTA-005 头文件整理与精简包 v2 验证

日期 2026-09-14；CHG-008 / TEST-008，结果 **PASS**。本记录只在本地保存。

变更：OTA 公共码移至 ota_update.h；温度/电量宏移至 temp_config.h；无线、主控制、OTA 会话、Bootloader 内部宏和类型分别移至 internal 头文件。调度器返回值命名为 SCH_RETURN_ERROR/NORMAL，公共头文件声明；保留现有错误报告开关语义。删除温度函数重复声明，BLE 的 OTA 状态码复用 OTA 公共定义。厂商代码及诊断程序不改。

验证：

- 7 个头文件独立/重复包含编译通过；原公共 API、持久化字段及运行状态数据保留。
- 从 v2 压缩包实际解压工程执行工厂和 v100/v101 构建，均 0 错误、0 警告。
- 原测试脚本的布局、向量、CRC、产物及 162 个中断恢复模拟通过。
- Bootloader、v100/v101 应用 BIN、v100 工厂 HEX、v101 OTA 及 manifest 与冻结件逐字节一致。
- 包内 178 个文件逐项核验，ZIP 读回通过，原 main 未被验证构建改写；旧精简包及 40 个冻结文件未变。
- 本次未烧录，实机 **NOT_RUN**。

交付：`TW66GW02_工程交接_精简版_v2.zip`，2361389 字节；SHA-256 `0b71345ae755f6d57e87134fdcffd667229f631708aa534e70921afe02fc640d`。

[详细记录](result.json)、[验证与打包脚本](verify_and_package.py)、[工厂日志](factory-build.log)、[OTA 构建日志](ota-build.log)、[OTA 检查](ota-tests.log)。header-0～6.log 为独立包含检查日志。

恢复：本目录 before-main.zip 保存修改前 175 个 main 文件；必要时先保存当前工作，在独立目录解压对照，同时撤回本次新增头文件。它不是可直接覆盖当前工程的命令，也不替代版本控制。

后续：交接使用 v2；维护遵循 main 编码规范。历史基线捕获/打包脚本含旧指纹保护，新源码不应强行绕过保护；使用本轮指纹记录新的变化。
