# 整理记录

[返回项目入口](../README.md) · [迁移计划](架构与迁移计划.md)

## 2026-09-10：第四步，交付目录与中间文件清理

四组 MCU DEV 快照按来源及版本归档；六个 APP/BLE 文件迁入交付目录，SHA-256 一致。每组 MCU 包附源码指纹、分区/版本说明、manifest 和校验记录。主线/14 的 1,951 个编译中间文件经备份后清理，清理后重建和 OTA 检查通过。详见[第四步报告](第四步交付整理与清理报告.md)。以下各阶段描述保留其当时状态。

## 2026-09-09～10：第三步，工程迁移与仓库边界

42 项目录/文件迁移完成：主工程迁入 01_固件工程/main，1–11 归档到 history，12/13/14 归入 development；TEST、ostest、eide 保持相邻关系归入实验工程，J-Link/J-Flash 归入工具目录。10 的占用由用户关闭后解除，已完成迁移。

修改链接和构建前，对 15,919 个文件逐一校验，内容全部一致；未读取的私钥/依赖目录随父目录整体迁移。根 .git 原位保留，12/13 独立仓库保留，14 在外层新建独立仓库但没有首次提交。本轮没有暂存、提交或推送。

main 工厂构建缺少被忽略的 boot_image.s，现由 PrepareFactoryBootImage.ps1 自动生成；修改后 Keil 构建 0 错误、0 警告。主线和 14 的 OTA 产物/162 项恢复检查通过；14 的 43 项脚本与 260 项 C 检查通过；v103/v104 应用编译通过，13 BLE 工具离线自检通过。12 的旧工厂入口不兼容当前分区、14 的工作簿测试失败属于保留并记录的原有问题，后者已用备份复现。

新入口、回滚材料、已执行验证和边界见 [第三步迁移与验证报告](第三步迁移与验证报告.md)。下方第一、二步记录保留各阶段当时的状态；当前工程已不在原数字目录。第四步现已完成，见本页最新记录。

## 2026-09-09：第二步，恢复备份与公共资料归集

已完成主线/共享文件、12、13、14 四份本地 ZIP 备份，总计 11,734 个文件。每个归档成员都已解压读取核对 SHA-256，并在备份完成时复核源文件；另外在隔离目录实际恢复了 12 个代表文件，包含源码、Git 元数据、旧文档和 13 内未提交的联调文件，指纹均一致。

备份范围与恢复方式见 [恢复说明](../90_历史原始包/整理前备份/20260909-step2/README.md)。这些是同磁盘恢复包：不包含 12/private_keys、13/14 的依赖环境及缓存，也不包含本轮保持原样的 1–11 快照。被排除目录未改动。备份包通过新增的精确目录规则排除于根仓库普通提交，原有 `.gitignore` 改动已保留。

已建立 02 需求与协议、03 硬件资料、04 测试与联调三个公共维护目录，共归集 24 份资料，详见 [源路径与指纹映射](证据/第二步资料映射.csv)。历史工程原件和根目录其他原始资料保留；根目录两份操作逻辑 Markdown 改为导航页，正文集中维护。解析资源目录一起归集，缺失图片改为待补说明，并用资源 README 保留目录。

05 发布与交付已建立现有文件登记，尚未迁移安装包或重新打包固件。包内元数据读取结果：Android 3.6.9 / build 368，iOS 3.6.9 / build 98；实际来源、适配固件和设备联调状态仍待核实。模块固件版本继续标为待核实，未从文件名推断。

验证：24 份公共资料的目标指纹一致；12/13/14 归档范围内 3,561 个当前文件与整理前指纹一致；第一步的 512 条源码/配置指纹也全部一致。维护页面的本地链接已检查，详细计数见 [第二步验证结果](证据/第二步验证结果.json)。没有编译、烧录或执行 OTA。

下一步为工程与仓库边界整理，尚未执行。1–14、eide/ostest、构建脚本和密钥路径均保持原位。本轮没有删除文件、清理缓存、提交 Git 或改变主线选择。

## 2026-09-09：第一步，入口与索引

新增根目录 README、版本总表、文件用途索引、架构与迁移计划、本记录，以及根目录/10/14 的所选源码指纹和比较摘要。为旧版本对比记录添加当前状态提示，保留历史正文和原始证据。

检查包括目录盘点、关键源码/工程/脚本静态核对、Git 本地分支及仓库边界查询、重复文档字节哈希比较、新增 Markdown 本地链接检查。未重新编译、执行测试固件、连接设备、烧录或做 OTA；历史构建报告的通过记录不是本轮验证结果。

本轮验证结果：5 份新增 Markdown 共检查 105 处本地链接，缺失目标为 0；源码指纹 CSV 共 512 条记录，重新读取文件核对 SHA-256，不一致为 0。根目录与 10 各选取 163 个工程文件，规范化后差异为 0；14 相对 10 新增 18 个工程文件、修改 56 个，外层辅助脚本另有新增 1 个、修改 2 个。新增入口、索引和指纹没有被当前根仓库忽略规则排除，但本轮没有暂存或提交文件。

整理前盘点参考值：排除根 `.git` 后 17,631 个文件、约 2,224.19 MiB；包含部分历史 Git 元数据与构建产物。这是此前本轮会话中的一次盘点值，不是实时计数或完整备份清单。`.o` 与 `.crf` 合计约 1,168.48 MiB，仅用于说明后续清理优先级。

本次再次搜索指令文件时，`12/private_keys` 与 `14/outputs/optimization/deps` 部分目录拒绝访问。没有为索引读取这些目录的内容；源码比较明确排除密钥、依赖、环境及构建输出。未发现适用于本次新增索引位置的 AGENTS.md。

## 第一步结束时的已知事项（历史状态）

| 项目 | 状态 |
| --- | --- |
| 根主线 / 10 / 14 定位 | 主线与 10 对应；14 已独立演进，见比较摘要 |
| 14 的备份 | 指纹已记录；可恢复源码备份尚未在本轮建立 |
| 14 的 CHANGELOG | 仍沿用 10 的内容；后续重构记录应基于实际差异单独补充 |
| UI 控件图片 | 解析文档引用的 ui_control_map.png 缺失，待补 |
| eide / ostest 关系 | 存在源码相对路径依赖，已纳入迁移计划 |
| 13 / 12 密钥路径关系 | 存在绝对路径依赖，已纳入迁移计划 |
| Git 保存范围 | 根仓库忽略数字目录；12/13 为独立仓库；不声称远端已备份 |
| 主线和版本状态 | 未变更主线选择，未指定量产版本 |

## 第一步边界（历史记录）

没有移动、删除、重命名现有工程或资料，没有更改 `.gitignore`、源码、构建脚本或 Git 历史。现有工作区改动继续保留。下一步从开发线可恢复保全和公共资料归集开始，具体顺序见迁移计划。
