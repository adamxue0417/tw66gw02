# 架构与迁移计划

[返回项目入口](../README.md) · [版本总表](版本总表.md) · [整理记录](整理记录.md)

采用“用途分区、版本归档、工程内部结构稳定”的方式。本计划优化工作区组织；固件模块重构是另一个工作范围，不在目录整理时顺带改变运行行为。

## 第一步：建立可用入口（已完成）

- [x] 根目录 README 提供工程、需求、联调和版本导航。
- [x] 建立版本总表，区分快照号、固件号、协议号、OTA 方案和验证状态。
- [x] 建立用途索引，覆盖现有一级目录与主要文件。
- [x] 为根目录、10、14 的所选源码和配置生成指纹与比较摘要。
- [x] 标出跨目录依赖、已有缺图和历史说明的适用日期。

这些修改不移动工程，不改构建路径，不运行烧录脚本。索引指纹不是备份。

## 第二步：保全开发线并归集公共资料（已完成）

1. 保全根目录已有未提交文件，以及 12、13 独立仓库和 14 当前工作副本。建立可恢复副本或仓库提交，并记录恢复入口；不能只保存哈希。
2. 建立 `02_需求与协议`、`03_硬件资料`、`04_测试与联调` 的实际目录，将公共资料按“原始需求 / 解析 / 协议 / 测试”归集。
3. 对重复文档采用一个公共维护副本；记录源路径、原名和 SHA-256。历史工程内副本先保留，不直接删除。
4. 将当前打开的解析文档与资源目录作为一组处理，修正全部入链和图片路径；缺失图片继续标注待补，不能以空图片替代。
5. 为 APK/IPA、BLE 模块固件补充来源、实际版本、适配固件和验证状态；未知字段写“待核实”。

完成情况：已生成四份经逐文件校验的本地恢复包，归集 24 份公共资料，保留来源和源/目标指纹，并核实 APP 包内版本。源码、配置、产物和仓库元数据可按归档范围恢复；私钥、依赖环境及未变更的 1–11 快照不包含在这组备份中，详见 [恢复说明](../90_历史原始包/整理前备份/20260909-step2/README.md)。工程位置未调整。

## 第三步：整理工程和仓库边界（已完成迁移，原有验证失败项见报告）

目标结构如下；00、02、03、04 已实际建立；05 已建立交付登记入口，90 已保存备份。01 固件工程、实验工程和 06 工具分类现已完成迁移。

```text
TW66GW02/
├─ README.md
├─ 00_项目索引/
├─ 01_固件工程/
│  ├─ main/
│  ├─ development/
│  │  ├─ snapshot-12_secure-ota/
│  │  ├─ snapshot-13_poweroff-v104/
│  │  └─ snapshot-14_refactor-from-10/
│  └─ history/
│     └─ snapshot-01～11_功能说明/
├─ 02_需求与协议/
├─ 03_硬件资料/
├─ 04_测试与联调/
│  └─ 实验工程/                 eide 与 ostest 保持相邻
├─ 05_发布与交付/
│  ├─ MCU固件/
│  ├─ BLE模块固件/
│  └─ 手机APP/
├─ 06_工具与参考/
└─ 90_历史原始包/
```

主仓库根 `.git` 已保持原位，主工程已迁到 `01_固件工程/main`，忽略规则同步更新。12、13 的嵌套仓库整体保留；14 在完整备份后于快照外层初始化独立仓库，尚无首次提交。根仓库继续忽略历史及独立开发线，没有把这些目录一次性加入根仓库。

每次只迁移一个完整工程，核对 Keil/CubeMX 源码引用、脚本定位、链接配置和测试资源路径，然后执行该工程适用的构建与回归。跨分区固件、Bootloader 和 APP 包一起记录兼容关系，不能以目录迁移代替固件升级验证。

## 已确认的依赖

| 依赖 | 整理时的处理 |
| --- | --- |
| [eide 配置](<../04_测试与联调/实验工程/eide/.eide/eide.yml>) 中大量 `../ostest/tw66gw02` 路径 | eide、ostest 同组移动并保持相邻，或完整更新配置后验证 |
| [13 的交付脚本](<../01_固件工程/development/snapshot-13_poweroff-v104/tw66gw02/BuildV104Delivery.ps1>) 默认 KeyRoot 指向 `12/private_keys` 的绝对路径 | 后续改为明确的外部参数/本地配置；先记录依赖，不复制密钥进公共资料或交付包 |
| 根目录和版本目录脚本使用 `$PSScriptRoot` 定位工程 | 构建、烧录脚本跟随工程；不能直接抽到公共 scripts 目录 |
| [14 的公共构建代码](<../01_固件工程/development/snapshot-14_refactor-from-10/tw66gw02/tools/Build.Common.ps1>)、[J-Link 公共代码](<../01_固件工程/development/snapshot-14_refactor-from-10/tw66gw02/tools/JLink.Common.ps1>) | 跟随 14 工程；先验证再考虑跨版本共用 |
| 14 外层工作簿脚本、tools、outputs | 按现有相对结构一起核对；outputs 内还有重构辅助源码和基线 |
| 旧版本分析 Markdown 中大量相对链接 | 若移动历史目录，逐一更新链接或维护明确的路径映射 |
| 根 `.gitignore` 使用 `/1/`、`/14/`、`MDK-ARM/` 等路径规则 | 工程移动时同步调整，检查不会意外跟踪构建产物或漏掉源码 |

## 第四步：交付规范与可重建文件清理（已完成，实机适配验证待补）

交付目录按“MCU / BLE 模块 / 手机 APP”分开。MCU 每个交付版本包含适配说明、固件、manifest 和验证记录；区分 Factory、App_Upload、Debug，保留 DEV 标识。版本来自构建参数和实际文件，不从快照号猜测。

每份交付索引至少记录：源码提交或指纹、固件版本、协议版本、Bootloader/分区、硬件适配、构建工具、文件 SHA-256、验证日期和状态。缺失信息明确标注待核实。

有可恢复备份且确认可重建后，再清理 `.o`、`.crf`、缓存及虚拟环境。保留 `.s` 启动源码、`.sct` 链接配置、历史 `.git.disabled`、已交付固件、manifest、验证日志和 14 的重构基线。不得按扩展名直接批量删除所有构建相关文件。

完成标准：交付包可追溯到源码和验证记录；清理不会移除唯一副本；迁移后的构建和索引验证通过。

执行结果：归集四组 MCU DEV 快照和六个手机/BLE 文件；提供源码指纹、历史来源限制、分区说明和文件校验。1,951 个指定编译中间文件经备份和逐文件解压校验后删除，主线/14 重建及 OTA 检查通过。依赖环境、历史快照和重构基线保留。统计、恢复方法与未验证项目见[第四步报告](第四步交付整理与清理报告.md)。

## 第三步执行结果

42 项目录/文件迁移完成，迁移后修改链接和构建前，15,919 个纳入指纹的文件全部一致。根 .git 留在原位；12/13 保留嵌套仓库；14 初始化独立仓库但没有提交。主线、14 和 v103/v104 的适用构建验证已执行，原有不兼容入口与失败测试见 [第三步报告](第三步迁移与验证报告.md)。第三步没有改变固件分区，也没有清理历史快照。
