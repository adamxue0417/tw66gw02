# TW66GW02 新工程师交接说明

交接编号：`TW66GW02_HANDOVER_20260914_v1`。这是开发交接快照，不是量产发布版本。

**主开发入口是 [01_固件工程/main](01_固件工程/main/README.md)，持续维护唯一的 [plan.md](plan.md)。** [AGENTS.md](AGENTS.md) 规定后续任务和证据如何更新。用 [HANDOVER.code-workspace](HANDOVER.code-workspace) 打开本包；不把历史 snapshot-10 或参考 snapshot-13 当成新的默认主线。

## 先读这四项

1. [固定计划的第 10 节](plan.md#10-下次工作入口)：已完成六步迁移准备，下一步按 B0/B1 开始真正的安全能力迁移；OTA-010 整体仍在进行。
2. [模块迁移清单与批次](00_项目索引/证据/OTA-010/20260914-step4-6/README.md)：哪些移植、哪些保留、哪些必须适配。
3. [main 构建结果](00_项目索引/证据/OTA-010/20260911-step2/README.md) 与 [snapshot-13 安全构建结果](00_项目索引/证据/OTA-010/20260911-step3/README.md)：区分当前代码、历史输入、离线通过和未执行实机项目。
4. [BLE 规范 v0.4 Draft](02_需求与协议/BLE协议/Mathis_BLE_Specification_v0.4_Draft.md)，重点 §9；[交付检查表](05_发布与交付/交付检查表.md)。

## 包里是什么

| 内容 | 定位 |
| --- | --- |
| main 完整受跟踪工程 | 主开发基线；175 个文件与第一步指纹一致 |
| snapshot-13 完整受跟踪参考工程 | 500 个文件与第一步指纹一致；包括来源构建所需固定 v103 输入和历史报告，不含 .venv/私钥 |
| 项目索引、需求协议、硬件、测试资料 | 保持原路径；操作逻辑解析文档与 assets 一起提供 |
| MCU 交付目录、BLE 固件和手机安装包 | 既有归档；版本/适配状态以 manifest 为准。手机包不是已经完成云端 OTA 的新 App |
| 第一步恢复 ZIP、前三步日志、模块清单 | 可追溯文件内容、验证范围和后续实施方法 |
| [DEV_public.pem](00_项目索引/交接/DEV_public.pem) 与 [DEV_public.der](00_项目索引/交接/DEV_public.der) | 仅公开验签材料；指纹见下方，可验证既有 DEV 包 |
| `handover-manifest.json`、`SHA256SUMS.txt`、`verify_handover.py` | 包内文件来源、大小、哈希和接收校验工具；由打包过程生成 |

当前 main 提交基线是 `b964654c6290591b0f4497e63704fa94161df99f`，原分支 `chore/workspace-organization`；snapshot-13 基线是 `d60b6a8654e6f3ac743a122699d7418b9cdc7d98`，原分支 `test/v104-poweroff-pc-ble`。**最新 plan、AGENTS、交接说明及证据属于本地工作区覆盖层，已打入本包，但未创建新的 Git 提交。** 不要仅克隆上述老提交就认为拿到了本包全部内容。

本包不含 `.git` 历史/远端凭证。接收者可在独立目录建立自己的初始提交并保存本包 SHA-256；如需既有历史，从原负责人提供的对应仓库分支获取后再核对覆盖层，不能将两个工程目录合并成同一个历史仓库。

## 接收和构建

解压到新的工作目录，优先使用较短路径。不要覆盖自己已有工程。先在包根目录运行：

```powershell
python .\verify_handover.py
```

校验会检查包内登记文件的大小/SHA-256、两个工程的基线文件和必备入口；之后开始工作产生新文件不影响原文件校验。修改文件后再校验出现不一致属于预期，应通过自己的 Git 管理后续变更。

构建环境：Windows PowerShell 5.1、Keil MDK Plus 5.39 / ARM Compiler **5.06 update 7 build 960**，默认安装位置 `C:\Keil_v5`；本轮离线测试使用 Python 3.14。PC BLE 工具安装脚本使用 Python 3.11 和 `bleak==3.0.2`，其离线协议自检不要求连接蓝牙。安全打包使用 OpenSSL 3.5.7（本机默认 `C:\Program Files\Git\usr\bin\openssl.exe`）。工具安装/许可需接收方准备。

在自己的工作副本中进入 main，执行：

```powershell
Set-Location .\01_固件工程\main
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\BuildKeilFactoryTarget.ps1
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\BuildOtaArtifacts.ps1 -FactoryVersion 100 -OtaVersion 101
python .\tests\test_ota_artifacts.py
```

以上只构建和检查，不烧录。当前预期：工厂/v100/v101 构建均 0 错误/0 警告，162 个中断恢复模拟通过。新输出在 main 的 OTA_Artifacts 中，仅作开发验证，不覆盖归档包。

snapshot-13 的安全重现使用 `BuildV104Delivery.ps1 -KeyRoot <受控 DEV 密钥目录>`。该目录需含原 DEV 私钥、PEM 公钥和 DER 公钥；不能用任意新密钥替换并期待设备接受。**包内只有公钥，不能完成重新签名；获取 DEV 私钥是另行受控交接事项，不是工程缺文件。** 可先在其工程内运行原 `tests/verify_signed_ota.ps1 -PublicKey <本包 DEV_public.pem 的绝对路径>`，验证包内历史 v103 签名；这不证明当前源码已重编译。

DEV 公钥 DER SPKI SHA-256：`3fccdcb727514f80a264fec76933c305262d3ce92f91f8a9711e4eec1a84c0d4`。生产私钥由对方团队保管，不向制造商/App/交接包提供。

## 测试和交付边界

- 第一阶段继续使用 [main_v100-to-v101_DEV](05_发布与交付/MCU固件/main_v100-to-v101_DEV)：完整 OTA SHA-256 为 `7d72822aa7ad78dde1265e720eed1b003e81f5e70d59e33c9e1a09ce0bb41b8e`。App 发送 App_Upload 的 OTA+manifest；工厂 HEX 和裸应用不能代替 OTA 文件。
- main 目前仍是 6 KiB Boot / App `0x08001800` 的开发 OTA，未开启验签/防降级；snapshot-13 是 8 KiB Boot / App `0x08002000` 的 DEV 安全参考线。两类工厂镜像和应用不混用。
- snapshot-13 离线 DEV 验证通过，但仍未启用 PROD，版本绑定待补。应用剩余空间 740 字节、Boot 主区 692 字节，新增能力要重新测量。
- 本轮没有上板。生产公钥、双公钥测试机、真实掉电/低电/防回滚、生产回包与上传验收仍按 plan 待执行。
- 两节电池 2.4 V 是邮件建议；采样比例/阈值尚待硬件确认，不直接修改为 2400。现有 OTA 电量准入为 30%。

## 未随包提供的资料与链接

不包含 history 全部源码、snapshot-12/14 独立源码仓库、实验工程、Git 对象库、厂商工具安装、旧手机源码工程、虚拟环境、私钥、前期大型整理备份以及隔离构建的中间目录。历史索引/报告可能链接到这些本地材料；包内 `handover-link-audit.json` 列出未携带目标，不能当作当前 main 缺少编译依赖。当前主入口、plan、迁移清单与基线源码均为必需项并单独校验。

旧报告中的绝对 `C:/Users/PC/...` 路径是历史运行记录，应以本机解压根目录替换理解；不要直接运行捕获脚本去覆盖旧证据。前三步恢复 ZIP 保留在原相对路径，含已知范围的文件内容，不是完整系统/工具环境备份。

实际联系人与联调信息尚待原负责人提供：测试机编号、板卡/BLE/手机组合、云端测试 App、生产公钥及签名包、上传接口、第二阶段正式版本号。请在 plan 的 DEC-004～009 原记录中补充，不另起新计划。
