> 2026-09-09 状态提示：本文保留 2026-09-07 的历史分析与复制校验。当前 `14` 已在复制后继续修改，不再与 `10` 完全相同；最新定位与核对证据见 [版本总表](00_项目索引/版本总表.md)。下文“全部一致”仅指复制完成时。

检查日期：2026-09-07。已完成 1–13 版本的源码、工程配置和主要构建脚本对比，并将整个 `10` 目录复制为 `14`。这些编号代表本地项目快照，不是持续累加的单一发布序列；其中存在功能撤回、独立 OTA 方案和目录缺失。

**14 版复制结果**

| 项目 | 结果 |
| --- | --- |
| 来源 | `C:\Users\PC\Desktop\TW66GW02\10` |
| 新目录 | `C:\Users\PC\Desktop\TW66GW02\14` |
| Keil 工程 | [14/tw66gw02/MDK-ARM/tw66gw02.uvprojx](<01_固件工程/development/snapshot-14_refactor-from-10/tw66gw02/MDK-ARM/tw66gw02.uvprojx>) |
| 复制范围 | 整个 10 目录，含项目、构建产物、outputs、协议文档、工作簿及脚本 |
| 文件数量 | 779 |
| 目录数量 | 44，包含版本目录自身 |
| 总字节 | 174,853,771，约 166.75 MiB |
| 校验 | 相对文件路径、大小和 SHA-256 全部一致，目录结构一致；缺失、额外、内容不同均为 0 |

14 版的固件版本宏、工程名和已有产物均沿用 10 版。文件夹命名为 `14` 不会自动将固件上报版本改成 14。根目录 `.gitignore` 按照现有 `/1/` 至 `/13/` 的惯例追加了 `/14/`。

完整校验结果见 [v14_copy_verification.json](.analysis/v14_copy_verification.json)，逐文件 SHA-256 见 [v14_sha256.csv](.analysis/v14_sha256.csv)。

**1–13 版本逐版差异**

| 版本 | 核实到的主要特点和变化 | 主要依据 |
| --- | --- | --- |
| 1 | 早期测温与显示基线：D/O/Cavity/Probe 切换，温度融合显示，旧 `FE … FF` 蓝牙协议代码，5 分钟未连接蓝牙的无操作关机计时。当前 `main.c` 中蓝牙任务注册被注释。 | `1/tw66gw02/COP/main_control.c`、`BSP/wireless.c`、`Core/Src/main.c` |
| 2 | 本地目录只剩 MDK-ARM 内容，不能作为完整源码工程。使用主仓库本地 `v2` 分支补充核对：与 1 版有 39 个文本变化文件，去除注释、空白后 C/H 代码 token 相同，主要是注释及格式整理。 | 本地 `v2` 提交 `93abbb5`；[1→2 差异](.analysis/1_to_2_git.diff) |
| 3 | 加强高温、低温、断线状态区分及去抖；PT1000 表扩展到 500°C；主通道断线但 Probe 有效时可显示 Probe；蓝牙键改为 1 秒长按切换开关。温度采集任务周期从 500 改为 900。 | [2→3 差异](.analysis/2_git_to_3.diff) |
| 4 | 主通道显示由多通道温度融合改为各自的四次多项式系数校准；新增 `0x60/0x61` 系数设置/查询；外部温度增加 10 点排序后取中间 4 点平均；Probe 温度条使用 10–90°C 档位，低温阈值改为 0°C；采集周期恢复 500。 | [3→4 差异](.analysis/3_to_4.diff) |
| 5 | 温度内部缓存统一保存摄氏度，显示时转换单位；调整温度条段位映射；补充旧协议状态、运行时间等回复字段（部分只是占位回复）；移除原 5 分钟自动关机计时，并调整蓝牙上电时序。 | [4→5 差异](.analysis/4_to_5.diff) |
| 6 | 独立的早期 BLE OTA 尝试：新增 Bootloader 和 `FE 40` OTA 接收，CRC16/CRC32、逐包 ACK；4 KiB Bootloader、29 KiB App、29 KiB staging，App 从 `0x08001000` 启动。蓝牙任务启用；同时恢复若干较早的温度/界面处理和 5 分钟关机逻辑，不是完整叠加 5 版。 | [5→6 差异](.analysis/5_to_6.diff)、[OTA_PARTITION.md](<01_固件工程/history/snapshot-06_early-ota/tw66gw02/OTA_PARTITION.md>) |
| 7 | 撤回 6 版的 Bootloader、`FE 40` OTA 和应用地址搬迁，恢复直接从 Flash 基址运行。与 5 版相比仅 3 个源码文件不同，主要是旧蓝牙协议回复和蓝牙电源操作；没有保留 5 版全部协议补充。蓝牙任务注册再次被注释。 | [6→7 差异](.analysis/6_to_7.diff)、[5→7 差异](.analysis/5_to_7.diff) |
| 8 | 大幅改用 Mathis `0x5A` 帧、CRC16、序号及 1 Hz 遥测；系数按 Float32 分包设置；单位与系数持久化，新增恢复出厂；自动关机改为 1 小时；电池电压使用 60 秒滑动平均；修正按住开机键时误关机，增加蓝牙键 10 秒操作。OTA 类型此时明确忽略。 | [7→8 差异](.analysis/7_to_8.diff) |
| 9 | 安全 OTA 概念验证方案：新增 Common 层以及版本目录旁的独立 `tw66gw02_bootloader`；RSA-3072/SHA-256、最低安全版本检查、事务式换页和回滚。13 KiB Bootloader、两个 24 KiB 槽，App 从 `0x08004000` 启动；设备协商 OTA 块为 128 字节。使用 DEV 测试密钥。 | [8→9 差异](.analysis/8_to_9.diff)、[OTA_IMPLEMENTATION.md](<01_固件工程/history/snapshot-09_secure-ota-experiment/OTA_IMPLEMENTATION.md>)、`9/tw66gw02_bootloader/Src/boot_crypto.c` |
| 10 | 另一套开发 OTA 实现：`ota_update.c` 和 `ota_boot.c`，6 KiB Bootloader、两个 27 KiB 槽，App 从 `0x08001800` 启动；232 字节逐包确认，换页恢复、试运行确认与回滚；配套 Keil 工厂镜像、启动诊断、J-Link 脚本、交付目录隔离及 SHA-256。384 字节签名尾为占位，未启用 RSA 验签和防降级。 | [9→10 差异](.analysis/9_to_10.diff)、[OTA_DEVELOPMENT_README.md](<01_固件工程/history/snapshot-10_development-ota/tw66gw02/OTA_DEVELOPMENT_README.md>) |
| 11 | 延续 10 版主要运行代码；GPIO 初始化避免将 PB3 电源保持脚先拉低，旧 OTA 地址宏改为有效分区别名。但打包脚本缺少 10 版的部分隔离/校验功能，而且当前目录缺少 4 个链接配置文件，详见下文。 | [10→11 差异](.analysis/10_to_11.diff) |
| 12 | 包含 v102 显示节电与 v103 安全 OTA 工作：C8721 恒流从 10 mA 调为 8.5 mA，显示 PWM 从 `0xFF` 调为 `0xD0`；增加 BearSSL RSA-3072/SHA-256 验签、防降级及安全烧录验证；Bootloader 改 8 KiB，App/staging 各 25 KiB，App 从 `0x08002000` 启动；调度任务槽 32→8，串口缓冲调整并减少复制，Heap 512→0。 | [11→12 差异](.analysis/11_to_12.diff)、[V103_MEMORY_REPORT.md](<01_固件工程/development/snapshot-12_secure-ota/tw66gw02/V103_MEMORY_REPORT.md>) |
| 13 | 在 12 版基础上加入 v104 关机改动与 Bootloader v2：SRAM 记录运行/允许重启/请求关机状态，按复位来源判断 PB3 保持；关机先让屏幕休眠、停止 BLE 和 DMA，再等待按键释放并拉低 PB3。增加 Windows 电脑 BLE 扫描、遥测、关机检查、OTA 工具及交付脚本。 | [12→13 差异](.analysis/12_to_13.diff)、[V104_BUILD_REPORT.md](<01_固件工程/development/snapshot-13_poweroff-v104/tw66gw02/V104_BUILD_REPORT.md>)、[电脑 BLE 工具说明](<01_固件工程/development/snapshot-13_poweroff-v104/tw66gw02/Tools/PC_BLE_Test/README.md>) |

**对使用和后续开发最有影响的区别**

1. **蓝牙协议在 8 版发生不兼容变化。** 1–7 的旧协议使用 `FE … FF`，8 版起的主协议使用 Mathis `0x5A` 帧。旧协议 App 不能直接按原帧格式控制 8–13。1、2（Git 源码）、3、4、5、7 的入口文件中 `SCH_Add_Task(WirelessTask, ...)` 被注释，存在协议实现文件不等于该任务已启用。
2. **9、10/11、12/13 的 OTA 内存布局与验签要求不同。** 9→10 切换了实现，同时撤去了 RSA 验签和最低安全版本检查；12 又加入新的安全实现。不能仅按文件后缀或目录编号混用这些工厂镜像、Bootloader 与 App OTA 包。
3. **11 版当前快照有明确的构建文件缺失。** 相比 10，缺少 `Bootloader/bootloader.sct`、`MDK-ARM/tw66gw02_app.sct`、`MDK-ARM/tw66gw02_direct.sct`、`MDK-ARM/tw66gw02_factory.sct`，而构建脚本/工程仍引用这些文件。按现有构建流程需要先补齐；本次只记录问题。
4. **11 版不是 10 版的完整增强集合。** `BuildOtaArtifacts.ps1` 缺少 10 版的 SHA-256 manifest 字段、Factory/App/Debug 交付目录隔离及相关校验；OTA/启动诊断临时构建也缺少 10 版对 AfterMake 的禁用处理。另一方面，11 的 PB3 初始化修改确实保留了电源保持，不能简单视为整个版本更旧或更差。
5. **12/13 的安全实现仍使用 DEV 密钥。** 12 的签名脚本默认引用 `12/private_keys`；13 的 `BuildV104Delivery.ps1` 默认继续引用该目录。13 的关机改动涉及 Bootloader，因此其联调说明要求先更新 Bootloader，再做 v103→v104 OTA，仅升级应用不能替换旧 Bootloader 的 PB3 逻辑。
6. **目录编号不等于固件上报版本。** 10–13 的 `BSP/config.h` 都保留 `MATHIS_FW_VERSION` 的默认值 100，构建脚本通过编译宏生成 101/102/103/104。14 本次完整继承 10 的默认值和脚本参数。
7. **显示节电与关机效果仍需要硬件确认。** 12 的历史记录给出的 LED 导通电流理论降幅约 30.7%，不是整机功耗实测值。13 的历史构建报告仍将真实 BLE、PB3 波形、关机电流/温升等列为待上板验证；本次没有新增实机验证。

分区对照如下，具体上限以各版头文件及链接配置为准：

| 版本 | Bootloader | App 起始地址 | App 槽 / staging 槽 | OTA 验证方式 |
| --- | --- | --- | --- | --- |
| 6 | 4 KiB | `0x08001000` | 29 / 29 KiB | CRC16/CRC32，参考 Bootloader 拷贝安装 |
| 9 | 13 KiB | `0x08004000` | 24 / 24 KiB | CRC32、RSA-3072/SHA-256、最低安全版本检查 |
| 10、11、14 | 6 KiB | `0x08001800` | 27 / 27 KiB | CRC32，384 字节开发签名占位 |
| 12、13 | 8 KiB | `0x08002000` | 25 / 25 KiB | CRC32、RSA-3072/SHA-256、防降级记录 |

**文件差异数量和检查范围**

下表是文本变化文件数，不是功能数量。比较前统一换行符与文本编码；保留其他空白和注释差异。范围是各版 `tw66gw02` 内的源码、头文件、启动汇编、链接配置、Keil/CubeMX 工程及构建/测试脚本；排除了 Git 元数据、RTE 自动文件、虚拟环境、编译输出和交付产物。9 版同级独立 Bootloader 和工具另行查看，其文件不计入下表。

| 比较 | 新增 | 修改 | 缺少/移除 | 说明 |
| --- | ---: | ---: | ---: | --- |
| 1→2（本地 Git） | 0 | 39 | 0 | C/H 代码 token 无差异，主要是注释/格式 |
| 2（本地 Git）→3 | 1 | 9 | 0 | 本地 2 目录不完整，使用已保存提交 |
| 3→4 | 0 | 7 | 0 | 温度校准与滤波 |
| 4→5 | 0 | 7 | 0 | 单位、显示、旧协议 |
| 5→6 | 2 | 14 | 0 | 早期 OTA 尝试及部分逻辑恢复 |
| 6→7 | 0 | 13 | 2 | 撤回 Bootloader 方案 |
| 7→8 | 0 | 16 | 0 | Mathis 协议及操作逻辑 |
| 8→9 | 6 | 9 | 2 | 另有同级 Bootloader/工具，未计入 |
| 9→10 | 25 | 10 | 7 | OTA 实现与工程构建方式变化 |
| 10→11 | 0 | 7 | 4 | 含 2 个仅文件末尾空行变化；4 个链接文件缺失 |
| 11→12 | 45 | 17 | 0 | 新增包含 BearSSL 源码及恢复的链接配置 |
| 12→13 | 7 | 19 | 0 | 另有 115 个仅编码/换行层面的字节差异 |

完整文件清单及每个变化文件的增删行数见 [version_comparison.json](.analysis/version_comparison.json)，各组逐行差异保存在 [.analysis](.analysis)。复核脚本为 [compare_versions.py](.analysis/compare_versions.py)。其中 `1_to_2.diff`、`2_to_3.diff` 仅记录不完整的本地 2 目录所造成的文件有无差异；分析第 2 版功能应查看 `1_to_2_git.diff`、`2_git_to_3.diff`。

第 1 版同级另有 `old v01`、`TW15A01-AS-software-V3.10` 参考工程，第 3–5 版带协议/操作逻辑文件，第 9 版带独立 Bootloader、参考工程和签名工具，第 10/12 版带工作簿及 outputs，第 13 版带电脑 BLE 工具。这些辅助目录不作为同一个 `tw66gw02` 主工程的连续源码版本统计。

本次完成的是静态源码/配置检查、现有文档交叉核对及完整复制校验；未重新编译各历史工程，也未烧录、连接设备或执行真实 OTA。已有文档中的“0 错误/0 警告”等属于历史验证记录，不作为本次重新测试的结论。原 1–13 版本目录保持原样。
