# v103内存与链接报告

## 结果

Keil ARMCC 5.06u7的最终v102安全基线和v103应用均为0错误、0警告：

| 项目 | v102原基线 | v103安全版 | 变化 |
| --- | ---: | ---: | ---: |
| Code | 22028 | 22336 | +308 |
| RO-data | 624 | 624 | 0 |
| RW-data | 240 | 280 | +40 |
| ZI-data | 5096 | 4832 | -264 |
| RW+ZI RAM | 5336 | 5112 | **-224** |

应用Flash实际BIN为23240字节，低于25216字节上限，余量1976字节。Bootloader有效代码区`ER_BOOT`为6984字节（`0x1B48`），固定Boot API为60字节；8 KiB镜像因固定API位于`0x08001FC0`而导出8188字节。

## RAM优化来源

- 调度器任务槽32降至8，当前6个任务有2个余量；同时修复添加前越界访问和删除索引校验。
- BLE USART1保留256字节，温度USART2降为32字节；DMA接收与完成缓冲通过指针交换转移所有权，不再复制到额外的256字节中间缓冲。
- 删除Wireless的`s_uart_chunk[256]`，Wireless直接解析完成缓冲。
- Heap由512字节降为0；工程没有`malloc/calloc/free`。
- 四个仅用于调试的累计计数收窄为16位。

## RSA栈

初版通用BearSSL i15路径的链接器最坏栈为2576字节，超过计划的2304字节。最终实现仍使用固定版本BearSSL i15算术和PKCS#1 v1.5解包，但将固定DEV公钥预编码为只读i15格式，并在模幂完成后复用临时区存放编码结果。最终链接器报告：

- `BootSecurity_VerifyArtifact`最坏调用栈：1688字节（`0x698`）；
- 应用栈：2304字节；
- 静态余量：616字节，超过“至少256字节”要求。

该数值来自ARM链接器调用图，不替代上板栈水印。OTA验签与中断叠加的动态水印仍须在实机验收时记录。

## 未改变项

显示内容和时序、温度滤波窗口、系数协议、按键/关机/自动关机、BLE帧格式与1 Hz遥测均未改变。内存优化没有通过缩短滤波窗口或删减显示功能换取空间。
