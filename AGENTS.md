# 项目维护入口

涉及 OTA、Bootloader、生产签名、固件交付或相关电量策略的工作，先阅读根目录 [plan.md](plan.md)，再核对所涉工程的实际代码、产物和局部说明。

- `plan.md` 是本次 OTA 工作的唯一主计划。按已有 `OTA-xxx` 编号继续，新增需求追加编号；不要从零重建计划或创建 `plan_v2.md` 等替代主计划。
- 开始工作先核对当前进度、任务依赖、待决事项和“下次工作入口”。只执行本轮用户授权的范围；计划中的后续任务不等于本轮全部执行。
- 变更约定时记录 `CHG-xxx`，更新受影响任务、接口和回归范围；新决策使用 `DEC-xxx`，失败使用 `ISSUE-xxx`，测试使用 `TEST-xxx`。保留原记录及证据。
- 完成工作后更新任务状态、测试实际结果、证据链接和下一条具体操作。任务只有满足全部验收条件才标记 `DONE`；发生回归时重新打开并保留历史证据。
- 明确区分当前源码、历史构建结果、本轮文件校验与实机结果。未做的实机测试写 `NOT_RUN`；依赖缺失的任务写 `BLOCKED` 并说明解除条件，不继承历史通过状态。
- 第一阶段冻结 main 原 v101 归档包，按计划中的哈希识别，不用同版本其他文件替换或重建。后续安全能力迁移到 main，历史安全开发线作为参考来源。
- 沿用现有仓库边界和交付检查表；保持 Bootloader、应用布局与交付件相匹配。公钥、布局、电压采样等未知条件按计划登记，不猜测为已确认。
- 生产私钥、上传凭证及带令牌 URL 不写入计划、代码、日志或交付目录。

本规则管理后续维护流程，不要求为已获授权的常规工作额外请求确认；用户的新指示应在相关记录中体现。

修改 main 自有固件代码时阅读 [编码规范](01_固件工程/main/编码规范.md)。公共接口、模块配置和私有定义分别维护；变量实体保留在 `.c`，不为整理定义而修改厂商代码或改变参数值。
